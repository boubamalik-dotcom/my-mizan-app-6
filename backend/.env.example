# Use PostgreSQL in production:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/el_mizan
# SQLite is the default for local bootstrap when DATABASE_URL is unset.
DATABASE_URL=sqlite:///./el_mizan.db
OPENAI_API_KEY=your_openai_api_key_here
DEBUG=true
